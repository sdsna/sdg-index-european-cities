class City {
  constructor({name,  score, rank, sdgs, region}) {
    this.name = name
    this.region = region
    this.score = score
    this.rank = rank
    this.sdgs = sdgs
  }
}

export default City
